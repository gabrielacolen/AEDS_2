class exercicio1 {
//3 0 0 0 or(and(A , B , C) , and(A , not(B) , C) , and(not(A) , not(B) , C) , and(not(A) , not(B) , not(C)))

    public static int algebraBooleana (String s){
       int qntVariaveis = s.charAt(0);
        
       if (qntVariaveis == 3){

            String newString = s.replace ('A', s.charAt(1));
            newString = s.replace ('B', s.charAt(2));
            newString = s.replace ('C', s.charAt(3));
            for (int i=0; i<s.length(); i++;){
                if(s.charAt(i) == 'a' && s.charAt(i) == 'n' && s.charAt(i) == 'd' ){
                    
                }
            }
            


       }
      
    }


    public static void main (String[] args){
    //     String[] entrada = new String[1000];
    //   int numEntrada = 0;

    //   //Leitura da entrada padrao
    //   do {
    //      entrada[numEntrada] = MyIO.readLine();
    //   } while (isFim(entrada[numEntrada++]) == false);
    //   numEntrada--;   //Desconsiderar ultima linha contendo a palavra FIM

    //   //Para cada linha de entrada, gerando uma de saida contendo o numero de letras maiusculas da entrada
    //     for(int i = 0; i < numEntrada; i++){
    //         if (isPalindromo(entrada[i]) == true) System.out.println("SIM");
    //         else  System.out.println("NAO");
    //   }

    }
}
